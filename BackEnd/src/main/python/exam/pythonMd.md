**range(start, stop, step) 有三個參數**
1. start（起始值，選填）： 
   * 默認值是 0，表示序列從 0 開始。 
   * 如果不提供 start，則序列的第一個數字是 0。
2. stop（結束值，**必填**）： 
   * 必須提供，表示序列的結束值（不包含此值）。
3. step（步長，選填）： 
   * 默認值是 1，表示每次增加（或減少）1。 
   * 如果不提供 step，則序列按默認的步長 1 遞增。